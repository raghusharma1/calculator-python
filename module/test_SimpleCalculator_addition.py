# ROOST_METHOD_HASH=SimpleCalculator.addition_9ccff787e3
# ROOST_METHOD_SIG_HASH=SimpleCalculator.addition_77ffd3333b

"""Tests for `calc.SimpleCalculator.addition`.

Auto-generated by roost-pytest. Re-generate when the source
changes; see the run report for generation details.
"""
# Source: calc.py:8-10
# Scenarios:
#   - [happy_path] add_positive_integers: Verify that adding two positive integers returns their sum.
#   - [happy_path] add_negative_integers: Verify that adding two negative integers returns their sum.
#   - [happy_path] add_floats: Verify that adding two floating-point numbers returns their sum.
#   - [edge_case] add_zero: Verify that adding zero to a number returns the number itself.
#   - [happy_path] add_mixed_positive_negative: Verify that adding a negative number and a positive number returns the correct sum.

# NB: roost-pytest could not get this test green.
#     target: calc.SimpleCalculator.addition
#     cause:  unknown__agent_stalled_without_running_pytest
#     note:   The pytest runner in this environment is failing with 'unrecognized arguments: --report-log', which prevents any tests from being collected or run. This appears to be a configuration issue in the prov
# This file is committed as a SKIPPED stub so the agent's draft
# is preserved for review. Either fix the failing scenarios and
# remove the pytestmark below, or delete the file outright. See
# the runreport.json for full diagnostics.

pytestmark = pytest.mark.skip(reason='agent could not produce a green test (unknown__agent_stalled_without_running_pytest)')

import pytest
from calc import SimpleCalculator

@pytest.mark.generated
@pytest.mark.happy_path
def test_add_positive_integers():
    """Verify that adding two positive integers returns their sum."""
    result = SimpleCalculator.addition(10, 5)
    assert result == 15

@pytest.mark.generated
@pytest.mark.happy_path
def test_add_negative_integers():
    """Verify that adding two negative integers returns their sum."""
    result = SimpleCalculator.addition(-10, -5)
    assert result == -15

@pytest.mark.generated
@pytest.mark.happy_path
def test_add_floats():
    """Verify that adding two floating-point numbers returns their sum."""
    result = SimpleCalculator.addition(1.5, 2.7)
    assert result == pytest.approx(4.2)

@pytest.mark.generated
@pytest.mark.edge_case
def test_add_zero():
    """Verify that adding zero to a number returns the number itself."""
    result = SimpleCalculator.addition(10, 0)
    assert result == 10

@pytest.mark.generated
@pytest.mark.happy_path
def test_add_mixed_positive_negative():
    """Verify that adding a negative number and a positive number returns the correct sum."""
    result = SimpleCalculator.addition(-5, 10)
    assert result == 5
